<Page x:Class="Wpf.Ui.Gallery.Views.Pages.DesignGuidance.ElectricalTTFPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
      xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Pages.DesignGuidance"
      xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="ElectricalTTFPage"
      controls:PageControlDocumentation.DocumentationType="{x:Type ui:ElectricalIcon}"
      d:DataContext="{d:DesignInstance local:ElectricalTTFPage,
                                       IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      ScrollViewer.CanContentScroll="False"
      mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ElectricalIconTemplate" DataType="{x:Type models:DisplayableIcon}">
            <Button Width="80" Height="80" Margin="2"
                    Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Command="{Binding ViewModel.IconSelectedCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:ElectricalTTFPage}}"
                    CommandParameter="{Binding Path=Id, Mode=OneTime}"
                    ToolTip="{Binding Name, Mode=OneTime}"
                    ToolTipService.InitialShowDelay="240">
                <ui:ElectricalIcon FontSize="48" Symbol="{Binding ElectricalIcon, Mode=OneTime}" />
            </Button>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TextBlock Grid.Row="0" Text="Electrical symbols from diagrams.net converted to TTF font format for use in WPF applications." TextWrapping="WrapWithOverflow" />
        <ui:HyperlinkButton Grid.Row="1" Margin="0,4,0,0" Padding="0"
                            Content="Source: diagrams.net electrical stencils" NavigateUri="https://github.com/jgraph/drawio/tree/dev/src/main/webapp/stencils/electrical" />
        <StackPanel Grid.Row="2" Margin="0,24,0,0" Orientation="Horizontal">
            <ui:TextBlock FontTypography="BodyStrong" Text="Electrical Symbols" />
        </StackPanel>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:AutoSuggestBox Grid.Column="0" MinWidth="320" Margin="0,4,0,0"
                               HorizontalAlignment="Left" PlaceholderText="Search symbols"
                               Text="{Binding ViewModel.AutoSuggestBoxText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    BorderBrush="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderThickness="1,1,0,0" CornerRadius="8,0,0,0">
                <ui:VirtualizingItemsControl Margin="0,24,4,0" Padding="0"
                                             ItemsSource="{Binding ViewModel.FilteredIconsCollection, Mode=OneWay}"
                                             ItemTemplate="{StaticResource ElectricalIconTemplate}"
                                             VirtualizingPanel.CacheLengthUnit="Pixel" />
            </Border>
            <Border Grid.Column="1" MinWidth="300"
                    Background="{ui:ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderThickness="0,1,1,0" CornerRadius="0,8,0,0">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <ui:TextBlock Grid.Row="0" FontTypography="BodyStrong"
                                  Text="{Binding ViewModel.SelectedSymbolName, Mode=OneWay}" />
                    <ui:ElectricalIcon Grid.Row="1" Margin="0,30,0,24" HorizontalAlignment="Left"
                                       FontSize="62"
                                       Symbol="{Binding ViewModel.SelectedElectricalSymbol, Mode=OneWay}" />
                    <ui:TextBlock Grid.Row="2" FontTypography="BodyStrong" Text="Symbol name" />
                    <ui:TextBlock Grid.Row="3"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{Binding ViewModel.SelectedSymbolName, Mode=OneWay}" />
                    <ui:TextBlock Grid.Row="4" Margin="0,8,0,0" FontTypography="BodyStrong"
                                  Text="Unicode point" />
                    <ui:TextBlock Grid.Row="5"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{Binding ViewModel.SelectedSymbolUnicodePoint, Mode=OneWay}" />
                    <ui:TextBlock Grid.Row="6" Margin="0,8,0,0" FontTypography="BodyStrong"
                                  Text="Text glyph" />
                    <ui:TextBlock Grid.Row="7"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{Binding ViewModel.SelectedSymbolTextGlyph, Mode=OneWay}" />
                    <ui:TextBlock Grid.Row="8" Margin="0,8,0,0" FontTypography="BodyStrong"
                                  Text="XAML" />
                    <ui:TextBlock Grid.Row="9" MaxWidth="250"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{Binding ViewModel.SelectedSymbolXaml, Mode=OneWay}"
                                  TextWrapping="WrapWithOverflow" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
