<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls">

    <ControlTemplate x:Key="controls.Ribbon.Templates.GalleryGroupContainer" TargetType="{x:Type controls:GalleryGroupContainer}">
        <DockPanel LastChildFill="True">
            <Grid
                x:Name="headerGrid"
                Height="21"
                VerticalAlignment="Top"
                Background="{DynamicResource Fluent.Ribbon.Brushes.GalleryGroupContainer.Header.Background}"
                DockPanel.Dock="Top">
                <TextBlock
                    Margin="7,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontWeight="Bold"
                    Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap" />
            </Grid>

            <controls:UniformGridWithItemSize
                HorizontalAlignment="Left"
                IsItemsHost="True"
                ItemHeight="{TemplateBinding ItemHeight}"
                ItemWidth="{TemplateBinding ItemWidth}"
                MaxColumns="{TemplateBinding MaxItemsInRow}"
                MinColumns="{TemplateBinding MinItemsInRow}"
                Orientation="{TemplateBinding Orientation}" />
        </DockPanel>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHeadered" Value="False">
                <Setter TargetName="headerGrid" Property="Visibility" Value="Collapsed" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="DefaultGalleryGroupContainerStyles" TargetType="{x:Type controls:GalleryGroupContainer}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.GalleryGroupContainer}" />
        <Style.Triggers>
            <Trigger Property="IsHeadered" Value="False">
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>