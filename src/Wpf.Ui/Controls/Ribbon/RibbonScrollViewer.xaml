<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
                    xmlns:conv="clr-namespace:Wpf.Ui.Controls.Converter"
                    >


    <!-- Button in Scroll Control Style -->
    <ControlTemplate x:Key="controls.Ribbon.Templates.ScrollButton"
                  TargetType="{x:Type RepeatButton}">
        <Border Background="{DynamicResource controls.Ribbon.Brushes.ScrollViewer.Button.Background}"
             CornerRadius="{Binding Path=(controls:RibbonProperties.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
            <Border x:Name="OuterBorder"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 Background="{DynamicResource controls.Ribbon.Brushes.ScrollViewer.Button.Background}"
                 BorderBrush="{DynamicResource controls.Ribbon.Brushes.ScrollViewer.Button.Border}"
                 BorderThickness="1"
                 CornerRadius="{Binding Path=(controls:RibbonProperties.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
                <ContentPresenter HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Background}" />
                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Border}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Background}" />
                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Border}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="DefaultScrollButtonStyle"
        TargetType="{x:Type RepeatButton}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource controls.Ribbon.Styles.FocusVisual.Empty}" />
        <Setter Property="Foreground" Value="{DynamicResource controls.Ribbon.Brushes.LabelText}" />
        <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.ScrollButton}" />
    </Style>

    <Style x:Key="DefaultRibbonScrollViewerStyle"
           TargetType="{x:Type ScrollViewer}">
        <Setter Property="CanContentScroll" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid"
                          Background="{TemplateBinding Background}"
                          IsHitTestVisible="True">
                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                Margin="{TemplateBinding Padding}"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                IsHitTestVisible="True" />
                        <RepeatButton Style="{DynamicResource DynamicScrollBarLineButton}"
                                      Width="13"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Stretch"
                                      Command="{x:Static ScrollBar.LineLeftCommand}"
                                      CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="false">
                            <controls:RibbonProperties.CornerRadius>
                                <Binding Converter="{x:Static conv:StaticConverters.CornerRadiusConverter}"
                                         Path="(controls:RibbonProperties.CornerRadius)"
                                         RelativeSource="{RelativeSource TemplatedParent}">
                                    <Binding.ConverterParameter>
                                        <conv:CornerRadiusPart>TopLeft,BottomLeft</conv:CornerRadiusPart>
                                    </Binding.ConverterParameter>
                                </Binding>
                            </controls:RibbonProperties.CornerRadius>
                            <RepeatButton.Visibility>
                                <MultiBinding Converter="{StaticResource MenuScrollingVisibilityConverter}"
                                              ConverterParameter="0"
                                              FallbackValue="Visibility.Collapsed">
                                    <Binding Path="ComputedHorizontalScrollBarVisibility"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="HorizontalOffset"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ExtentWidth"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ViewportWidth"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                </MultiBinding>
                            </RepeatButton.Visibility>
                            <Path Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.LeftRight}"
                                  Data="{DynamicResource controls.Ribbon.Values.Arrow.Left}"
                                  Stroke="{DynamicResource controls.Ribbon.Brushes.Black}" />
                        </RepeatButton>
                        <RepeatButton Style="{DynamicResource DefaultScrollButtonStyle}"
                                      Width="13"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Stretch"
                                      Command="{x:Static ScrollBar.LineRightCommand}"
                                      CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="false">
                            <controls:RibbonProperties.CornerRadius>
                                <Binding Converter="{x:Static conv:StaticConverters.CornerRadiusConverter}"
                                         Path="(controls:RibbonProperties.CornerRadius)"
                                         RelativeSource="{RelativeSource TemplatedParent}">
                                    <Binding.ConverterParameter>
                                        <conv:CornerRadiusPart>TopRight,BottomRight</conv:CornerRadiusPart>
                                    </Binding.ConverterParameter>
                                </Binding>
                            </controls:RibbonProperties.CornerRadius>
                            <RepeatButton.Visibility>
                                <MultiBinding Converter="{StaticResource MenuScrollingVisibilityConverter}"
                                              ConverterParameter="100"
                                              FallbackValue="Visibility.Collapsed">
                                    <Binding Path="ComputedHorizontalScrollBarVisibility"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="HorizontalOffset"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ExtentWidth"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ViewportWidth"
                                             RelativeSource="{RelativeSource TemplatedParent}" />
                                </MultiBinding>
                            </RepeatButton.Visibility>
                            <Path Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.LeftRight}"
                                  Data="{DynamicResource controls.Ribbon.Values.Arrow.Right}"
                                  Stroke="{DynamicResource controls.Ribbon.Brushes.Black}" />
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style x:Key="DefaultRibbonTabScrollViewerStyle"
          TargetType="{x:Type ScrollViewer}">
        <Setter Property="CanContentScroll" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid"
                         Background="{TemplateBinding Background}"
                         IsHitTestVisible="True">
                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                               Margin="{TemplateBinding Padding}"
                                               CanContentScroll="{TemplateBinding CanContentScroll}"
                                               Content="{TemplateBinding Content}"
                                               ContentTemplate="{TemplateBinding ContentTemplate}"
                                               IsHitTestVisible="True" />
                        <RepeatButton Style="{StaticResource DefaultScrollButtonStyle}"
                                     Width="13"
                                     Margin="0"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Stretch"
                                     Command="{x:Static ScrollBar.LineLeftCommand}"
                                     CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                     Focusable="false">
                            <RepeatButton.Visibility>
                                <MultiBinding Converter="{StaticResource controls.Ribbon.Converters.MenuScrollingVisibilityConverter}"
                                             ConverterParameter="0"
                                             FallbackValue="Visibility.Collapsed">
                                    <Binding Path="ComputedHorizontalScrollBarVisibility"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="HorizontalOffset"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ExtentWidth"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ViewportWidth"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                </MultiBinding>
                            </RepeatButton.Visibility>
                            <Path Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.LeftRight}"
                                 Data="{DynamicResource controls.Ribbon.Values.Arrow.Left}"
                                 Stroke="{DynamicResource controls.Ribbon.Brushes.Black}" />
                        </RepeatButton>
                        <RepeatButton Style="{StaticResource DefaultScrollButtonStyle}"
                                     Width="13"
                                     Margin="0"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Stretch"
                                     Command="{x:Static ScrollBar.LineRightCommand}"
                                     CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                     Focusable="false">
                            <RepeatButton.Visibility>
                                <MultiBinding Converter="{StaticResource controls.Ribbon.Converters.MenuScrollingVisibilityConverter}"
                                             ConverterParameter="100"
                                             FallbackValue="Visibility.Collapsed">
                                    <Binding Path="ComputedHorizontalScrollBarVisibility"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="HorizontalOffset"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ExtentWidth"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                    <Binding Path="ViewportWidth"
                                            RelativeSource="{RelativeSource TemplatedParent}" />
                                </MultiBinding>
                            </RepeatButton.Visibility>
                            <Path Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.LeftRight}"
                                 Data="{DynamicResource controls.Ribbon.Values.Arrow.Right}"
                                 Stroke="{DynamicResource controls.Ribbon.Brushes.Black}" />
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style x:Key="DefaultRibbonGroupsContainerScrollViewerStyles"
        TargetType="{x:Type controls:RibbonGroupsContainerScrollViewer}"
        BasedOn="{StaticResource DefaultRibbonScrollViewerStyle}">
        <Setter Property="controls:RibbonProperties.CornerRadius" Value="{DynamicResource RibbonCornerRadius}" />
    </Style>
</ResourceDictionary>