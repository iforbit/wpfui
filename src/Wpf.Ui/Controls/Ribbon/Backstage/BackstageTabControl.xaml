<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
                    xmlns:converters="clr-namespace:Wpf.Ui.Converters">

    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

    <!--  Back Button Template  -->
    <ControlTemplate x:Key="controls.Ribbon.Templates.BackstageBackButton" TargetType="Button">
        <Border x:Name="ContentBorder"
                MinWidth="48" MinHeight="48" Background="Transparent">
            <ContentControl Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="Left" Focusable="False">
                <Grid>
                    <Path x:Name="arrowpath"
                          Width="16" Height="12" Data="F1 M 36,22L 36,26L 19.75,26L 27,33L 20.5,33L 11,24L 20.5,15L 27,15L 19.75,22L 36,22 Z "
                          Fill="{TemplateBinding Foreground}"
                          Stretch="Uniform" />

                    <Ellipse x:Name="backellipse"
                             Width="34" Height="34"
                             Stroke="{TemplateBinding Foreground}"
                             StrokeThickness="2" />
                </Grid>
            </ContentControl>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="arrowpath" Property="Opacity" Value="0.8" />
                <Setter TargetName="backellipse" Property="Opacity" Value="0.8" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="arrowpath" Property="Opacity" Value="0.8" />
                <Setter TargetName="backellipse" Property="Opacity" Value="0.8" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Back Button Style  -->
    <Style x:Key="controls.Ribbon.Styles.BackstageBackButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Height" Value="48" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.BackstageBackButton}" />
    </Style>

    <!--  BackstageTabControl Template  -->
    <ControlTemplate x:Key="controls.Ribbon.Templates.BackstageTabControl" TargetType="{x:Type controls:BackstageTabControl}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="{TemplateBinding ItemsPanelMinWidth}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid x:Name="PART_ItemsPanelContainer"
                  VerticalAlignment="Stretch"
                  Background="{TemplateBinding ItemsPanelBackground}"
                  KeyboardNavigation.DirectionalNavigation="Cycle">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Button x:Name="PART_BackButton"
                        Grid.Row="0" Padding="22,7,0,7" VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        Command="{x:Static controls:RibbonCommands.OpenBackstage}"
                        Style="{DynamicResource controls.Ribbon.Styles.BackstageBackButton}"
                        Visibility="{Binding Path=IsBackButtonVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}" />

                <ItemsPresenter Grid.Row="1" />
            </Grid>

            <Grid x:Name="PART_SelectedContentGrid"
                  Grid.Column="1"
                  Background="{TemplateBinding Background}">
                <ContentPresenter x:Name="PART_SelectedContentHost"
                                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ContentSource="SelectedContent" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <!--  BackstageTabControl Style  -->
    <Style x:Key="controls.Ribbon.Styles.BackstageTabControl" TargetType="{x:Type controls:BackstageTabControl}">
        <Setter Property="Background" Value="{DynamicResource SolidBackgroundFillColorBaseBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="ItemsPanelBackground" Value="{DynamicResource SolidBackgroundFillColorBaseBrush}" />
        <Setter Property="ItemsPanelMinWidth" Value="125" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.BackstageTabControl}" />
    </Style>

    <!--  Default style  -->
    <Style BasedOn="{StaticResource controls.Ribbon.Styles.BackstageTabControl}" TargetType="{x:Type controls:BackstageTabControl}" />
</ResourceDictionary>
