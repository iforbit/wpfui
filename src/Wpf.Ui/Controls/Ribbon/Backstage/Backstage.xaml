<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Wpf.Ui.Controls">

    <!--  Storyboards for Backstage open/close animations  -->
    <Storyboard x:Key="Wpf.Ui.Storyboards.Backstage.IsOpenTrueStoryboard">
        <ThicknessAnimationUsingKeyFrames Storyboard.TargetProperty="(FrameworkElement.Margin)">
            <EasingThicknessKeyFrame KeyTime="0" Value="-125,0,0,0" />
            <EasingThicknessKeyFrame KeyTime="0:0:0.5" Value="0">
                <EasingThicknessKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </EasingThicknessKeyFrame.EasingFunction>
            </EasingThicknessKeyFrame>
        </ThicknessAnimationUsingKeyFrames>
    </Storyboard>

    <Storyboard x:Key="Wpf.Ui.Storyboards.Backstage.IsOpenFalseStoryboard">
        <ThicknessAnimationUsingKeyFrames Storyboard.TargetProperty="(FrameworkElement.Margin)">
            <EasingThicknessKeyFrame KeyTime="0" Value="0" />
            <EasingThicknessKeyFrame KeyTime="0:0:0.3" Value="-125,0,0,0">
                <EasingThicknessKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </EasingThicknessKeyFrame.EasingFunction>
            </EasingThicknessKeyFrame>
        </ThicknessAnimationUsingKeyFrames>
    </Storyboard>

    <!--  Backstage Toggle Button Template  -->
    <ControlTemplate x:Key="controls.Ribbon.Templates.BackstageToggleButton" TargetType="{x:Type controls:Backstage}">
        <Grid x:Name="root"
              Background="{TemplateBinding Background}"
              ToolTip="{TemplateBinding ToolTip}">
            <Border x:Name="underlineBorder"
                    Width="{Binding ElementName=contentPresenter, Path=ActualWidth}"
                    Margin="0,0,0,3" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                    Background="Transparent"
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                    BorderThickness="0,0,0,2" Visibility="Collapsed" />

            <ContentPresenter x:Name="contentPresenter"
                              Margin="9,3,9,6" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                              ContentSource="Header" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="underlineBorder" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="root" Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Backstage Style  -->
    <Style x:Key="controls.Ribbon.Styles.RibbonBackstage" TargetType="{x:Type controls:Backstage}">
        <Setter Property="Background" Value="{DynamicResource ApplicationBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Header" Value="File" />
        <Setter Property="KeyboardNavigation.TabIndex" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.BackstageToggleButton}" />
        <Setter Property="ToolTipService.IsEnabled" Value="False" />
    </Style>

    <!--  Default style  -->
    <Style BasedOn="{StaticResource controls.Ribbon.Styles.RibbonBackstage}" TargetType="{x:Type controls:Backstage}" />
</ResourceDictionary>
