<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <Thickness x:Key="MacAddressTextBoxBorderThemeThickness">1,1,1,0</Thickness>
    <Thickness x:Key="MacAddressTextBoxAccentBorderThemeThickness">0,0,0,1</Thickness>
    <Thickness x:Key="MacAddressTextBoxLeftIconMargin">10,8,0,0</Thickness>
    <Thickness x:Key="MacAddressTextBoxRightIconMargin">0,8,10,0</Thickness>
    <Thickness x:Key="MacAddressTextBoxButtonMargin">0,5,4,0</Thickness>
    <Thickness x:Key="MacAddressTextBoxButtonPadding">0,0,0,0</Thickness>
    <system:Double x:Key="MacAddressTextBoxButtonHeight">24</system:Double>
    <system:Double x:Key="MacAddressTextBoxButtonIconSize">14</system:Double>


    <Style x:Key="DefaultMacAddressTextBoxStyle" TargetType="{x:Type controls:MacAddressTextBox}">
        <!--  Universal WPF UI focus  -->
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <!--  Universal WPF UI focus  -->
        <Setter Property="Foreground" Value="{DynamicResource TextControlForeground}" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextControlForeground}" />
        <Setter Property="Background" Value="{DynamicResource TextControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextControlElevationBorderBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource MacAddressTextBoxBorderThemeThickness}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="MinHeight" Value="{DynamicResource TextControlThemeMinHeight}" />
        <Setter Property="MinWidth" Value="{DynamicResource TextControlThemeMinWidth}" />
        <Setter Property="Padding" Value="{DynamicResource TextControlThemePadding}" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="ClearButtonEnabled" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:MacAddressTextBox}">
                    <Grid HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <Border x:Name="ContentBorder"
                                MinWidth="{TemplateBinding MinWidth}"
                                MinHeight="{TemplateBinding MinHeight}"
                                Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding Border.CornerRadius}">
                            <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="1" Margin="{TemplateBinding Padding}">
                                    <controls:PassiveScrollViewer x:Name="PART_ContentHost"
                                                                  Style="{DynamicResource DefaultTextBoxScrollViewerStyle}"
                                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                                    <!--  Watermark TextBlock: Text이 없을 때 표시  -->
                                    <TextBlock x:Name="WatermarkTextBlock"
                                               Margin="0" Padding="1,0" VerticalAlignment="Top"
                                               Foreground="{DynamicResource TextControlPlaceholderForeground}"
                                               Text="{TemplateBinding Watermark}"
                                               Visibility="Collapsed" />
                                </Grid>
                                <controls:Button x:Name="ClearButton"
                                                 Grid.Column="2"
                                                 Width="{StaticResource MacAddressTextBoxButtonHeight}"
                                                 Height="{StaticResource MacAddressTextBoxButtonHeight}"
                                                 Margin="{StaticResource MacAddressTextBoxButtonMargin}"
                                                 Padding="{StaticResource MacAddressTextBoxButtonPadding}"
                                                 HorizontalAlignment="Center" VerticalAlignment="Top" HorizontalContentAlignment="Center"
                                                 VerticalContentAlignment="Center" Appearance="Secondary" Background="Transparent"
                                                 BorderBrush="Transparent"
                                                 Command="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                                 CommandParameter="clear" Cursor="Arrow"
                                                 Foreground="{DynamicResource TextControlButtonForeground}"
                                                 IsTabStop="False">
                                    <controls:Button.Icon>
                                        <controls:SymbolIcon FontSize="{StaticResource MacAddressTextBoxButtonIconSize}" Symbol="Dismiss24" />
                                    </controls:Button.Icon>
                                </controls:Button>

                            </Grid>
                        </Border>
                        <!--  The Accent Border is a separate element so that changes to the border thickness do not affect the position of the element  -->
                        <Border x:Name="AccentBorder"
                                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="{StaticResource MacAddressTextBoxAccentBorderThemeThickness}"
                                CornerRadius="{TemplateBinding Border.CornerRadius}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!--  Text가 비어 있으면 워터마크 표시  -->
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="WatermarkTextBlock" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="CurrentPlaceholderEnabled" Value="False">
                            <Setter TargetName="WatermarkTextBlock" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowClearButton" Value="False">
                            <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ClearButton" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="ClearButtonEnabled" Value="False">
                            <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ClearButton" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="AccentBorder" Property="BorderThickness" Value="0,0,0,2" />
                            <Setter TargetName="AccentBorder" Property="BorderBrush" Value="{DynamicResource TextControlFocusedBorderBrush}" />
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextControlBackgroundFocused}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextControlBackgroundPointerOver}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter Property="Cursor" Value="IBeam" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextControlBackgroundDisabled}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushDisabled}" />
                            <Setter TargetName="AccentBorder" Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushDisabled}" />
                            <Setter TargetName="WatermarkTextBlock" Property="Foreground" Value="{DynamicResource TextControlForegroundDisabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextControlForegroundDisabled}" />
                            <Setter Property="ClearButtonEnabled" Value="False" />
                        </Trigger>
                        <!--  IsComplete가 false일 때 (12자리 미만) 배경색을 LightCoral로 변경  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="HasUserInput" Value="True"/>
                                <Condition Property="IsComplete" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{DynamicResource SystemFillColorCautionBackgroundBrush}" />
                        </MultiTrigger>
                        <!-- 입력이 완료된 경우 (원하는 경우 다른 스타일 적용) -->
                        <!--<Trigger Property="IsComplete" Value="True">
                            <Setter Property="Background" Value="LightGreen"/>
                        </Trigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultMacAddressTextBoxStyle}" TargetType="{x:Type controls:MacAddressTextBox}" />

</ResourceDictionary>