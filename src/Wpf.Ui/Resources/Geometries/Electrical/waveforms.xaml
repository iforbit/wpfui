<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Waveforms Symbols - Auto-generated from draw.io stencils -->

    <!-- Data Changing (90x90) -->
    <Geometry x:Key="Waveforms.Data_Changing">
        M0,90 L90,0 M0,0 L90,90
    </Geometry>

    <!-- Data Ending (95x90) -->
    <Geometry x:Key="Waveforms.Data_Ending">
        M55,45 L70,45 M85,45 L95,45 M0,0 L45,45 L0,90
    </Geometry>

    <!-- Data Invalid 1 (100x40) -->
    <Geometry x:Key="Waveforms.Data_Invalid_1">
        M0,0 L15,0 M30,0 L45,0 M60,0 L75,0 M90,0 L100,0 M0,40 L15,40 M30,40 L45,40 M60,40 L75,40 M90,40 L100,40
    </Geometry>

    <!-- Data Invalid 2 (100x5) -->
    <Geometry x:Key="Waveforms.Data_Invalid_2">
        M0,2.5 L15,2.5 M30,2.5 L45,2.5 M60,2.5 L75,2.5 M90,2.5 L100,2.5
    </Geometry>

    <!-- Data Starting (95x90) -->
    <Geometry x:Key="Waveforms.Data_Starting">
        M0,45 L15,45 M30,45 L45,45 M95,0 L50,45 L95,90
    </Geometry>

    <!-- Data Valid (100x40) -->
    <Geometry x:Key="Waveforms.Data_Valid">
        M0,0 L100,0 M0,40 L100,40
    </Geometry>

    <!-- Down Ramp (90x90) -->
    <Geometry x:Key="Waveforms.Down_Ramp">
        M0,0 L90,90
    </Geometry>

    <!-- Edge (5x90) -->
    <Geometry x:Key="Waveforms.Edge">
        M2.5,0 L2.5,90
    </Geometry>

    <!-- Event (25.56x55.22) -->
    <Geometry x:Key="Waveforms.Event">
        M0.0,50.22 A5.0,5.0 0 0 1 5.0,45.22 A5.0,5.0 0 0 1 10.0,50.22 A5.0,5.0 0 0 1 5.0,55.22 A5.0,5.0 0 0 1 0.0,50.22 Z M25,0.22 A60,60 0 0 0 5,50.22 M21.67,6 L25.56,0 L19,3.67 Z
    </Geometry>

    <!-- Level (90x5) -->
    <Geometry x:Key="Waveforms.Level">
        M0,2.5 L90,2.5
    </Geometry>

    <!-- Pulse 1 (90x90) -->
    <Geometry x:Key="Waveforms.Pulse_1">
        M0,90 L25,90 L25,0 L65,0 L65,90 L90,90
    </Geometry>

    <!-- Pulse 2 (90x90) -->
    <Geometry x:Key="Waveforms.Pulse_2">
        M0,0 L25,0 L25,90 L65,90 L65,0 L90,0
    </Geometry>

    <!-- Sawtooth (100x100) -->
    <Geometry x:Key="Waveforms.Sawtooth">
        M0,100 L33.33,0 L33.33,100 L66.67,0 L66.67,100 L100,0 L100,100
    </Geometry>

    <!-- Sine Wave (89.75x88.17) -->
    <Geometry x:Key="Waveforms.Sine_Wave">
        M0,43.95 C18.26,0.23 26.87,0 44.87,43.95 C62.88,87.9 71.32,88.17 89.75,43.95
    </Geometry>

    <!-- Slow Square Wave (90x80) -->
    <Geometry x:Key="Waveforms.Slow_Square_Wave">
        M0,40 L10,0 L35,0 L55,80 L80,80 L90,40
    </Geometry>

    <!-- Square Wave (90x80) -->
    <Geometry x:Key="Waveforms.Square_Wave">
        M0,40 L0,0 L45,0 L45,80 L90,80 L90,40
    </Geometry>

    <!-- Step 1 (90x90) -->
    <Geometry x:Key="Waveforms.Step_1">
        M0,90 L45,90 L45,0 L90,0
    </Geometry>

    <!-- Step 2 (90x90) -->
    <Geometry x:Key="Waveforms.Step_2">
        M0,0 L45,0 L45,90 L90,90
    </Geometry>

    <!-- Timing Line (98x10) -->
    <Geometry x:Key="Waveforms.Timing_Line">
        M1,5 L97,5 M89,0 L98,5 L89,10 M9,0 L0,5 L9,10
    </Geometry>

    <!-- Timing Mark 1 (5x100) -->
    <Geometry x:Key="Waveforms.Timing_Mark_1">
        M2.5,0 L2.5,100
    </Geometry>

    <!-- Timing Mark 2 (100x100) -->
    <Geometry x:Key="Waveforms.Timing_Mark_2">
        M50,0 L50,100 M0,50 L100,50
    </Geometry>

    <!-- Timing Mark 3 (100x5) -->
    <Geometry x:Key="Waveforms.Timing_Mark_3">
        M0,2.5 L100,2.5
    </Geometry>

    <!-- Up Ramp (90x90) -->
    <Geometry x:Key="Waveforms.Up_Ramp">
        M0,90 L90,0
    </Geometry>

</ResourceDictionary>