<!--
    This code was generated by a tool.
    Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
-->
<!--
Source files:
Controls\Ribbon\Converters.xaml
Controls\Ribbon\FocusStyles.xaml
Controls\Ribbon\MenuSeparator.xaml
Controls\Ribbon\Ribbon.xaml
Controls\Ribbon\RibbonCommon.xaml
Controls\Ribbon\RibbonGroupBox.xaml
Controls\Ribbon\RibbonScrollViewer.xaml
Controls\Ribbon\RibbonTabControl.xaml
Controls\Ribbon\RibbonTabItem.xaml
Controls\Ribbon\SeparatorTabItem.xaml
Controls\Ribbon\TransitioningControl.xaml
Controls\Ribbon\TwoLineLabel.xaml
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:Wpf.Ui.Controls" xmlns:internal="clr-namespace:Wpf.Ui.Internal" xmlns:conv="clr-namespace:Wpf.Ui.Controls.Converter" xmlns:templateSelectors="clr-namespace:Wpf.Ui.Controls.TemplateSelectors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
  <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
  <MenuScrollingVisibilityConverter x:Key="MenuScrollingVisibilityConverter" />
  <ControlTemplate x:Key="controls.Ribbon.Templates.FocusVisual.Empty" TargetType="{x:Type Control}">
    <Rectangle Stroke="Transparent">
      <Rectangle.Fill>
        <SolidColorBrush />
      </Rectangle.Fill>
    </Rectangle>
  </ControlTemplate>
  <ControlTemplate x:Key="controls.Templates.Ribbon" TargetType="{x:Type controls:Ribbon}">
    <ControlTemplate.Resources />
    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
      <Grid x:Name="PART_LayoutRoot">
        <controls:RibbonTabControl x:Name="PART_RibbonTabControl" AreTabHeadersVisible="{TemplateBinding AreTabHeadersVisible}" ContentHeight="{TemplateBinding ContentHeight}" />
      </Grid>
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="IsCollapsed" Value="True">
        <Setter TargetName="PART_RibbonTabControl" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger Property="IsSimplified" Value="True">
        <Setter TargetName="PART_RibbonTabControl" Property="ContentHeight" Value="42" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="DefaultRibbonTabScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
    <Setter Property="CanContentScroll" Value="True" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollViewer}">
          <Grid x:Name="Grid" Background="{TemplateBinding Background}" IsHitTestVisible="True">
            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Margin="{TemplateBinding Padding}" CanContentScroll="{TemplateBinding CanContentScroll}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" IsHitTestVisible="True" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
  </Style>
  <Thickness x:Key="controls.Ribbon.Values.Default.Margin">1 1 1 1</Thickness>
  <Thickness x:Key="controls.Ribbon.Values.Default.Padding">2 0 2 0</Thickness>
  <Thickness x:Key="controls.Ribbon.Values.RibbonTabControl.Content.BorderThickness">0</Thickness>
  <Thickness x:Key="controls.Ribbon.Values.RibbonTabControl.Content.Margin">8 0 8 0</Thickness>
  <Thickness x:Key="controls.Ribbon.Values.RibbonTabItem.Active.BorderThickness">0 0 0 2</Thickness>
  <GridLength x:Key="controls.Ribbon.Values.MenuItem.IconColumnWidth">25</GridLength>
  <PathGeometry x:Key="controls.Ribbon.Values.Arrow.Left">M 0 0 L -5 5 L 0 10</PathGeometry>
  <PathGeometry x:Key="controls.Ribbon.Values.Arrow.Up">M 0 0 L -5 -5 L -10 0</PathGeometry>
  <PathGeometry x:Key="controls.Ribbon.Values.Arrow.Right">M 0 0 L 5 -5 L -0 -10</PathGeometry>
  <PathGeometry x:Key="controls.Ribbon.Values.Arrow.Down">M 0 0 L 5 5 L 10 0</PathGeometry>
  <Style x:Key="controls.Ribbon.Styles.Path.Arrow.UpDown" TargetType="Path">
    <Setter Property="Height" Value="6" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="Stretch" Value="Uniform" />
    <Setter Property="StrokeDashCap" Value="Flat" />
    <Setter Property="StrokeEndLineCap" Value="Flat" />
    <Setter Property="StrokeLineJoin" Value="Round" />
    <Setter Property="StrokeStartLineCap" Value="Flat" />
    <Setter Property="StrokeThickness" Value="2" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Width" Value="10" />
  </Style>
  <Style x:Key="controls.Ribbon.Styles.Path.Arrow.LeftRight" TargetType="Path">
    <Setter Property="Height" Value="10" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="Stretch" Value="Uniform" />
    <Setter Property="StrokeDashCap" Value="Flat" />
    <Setter Property="StrokeEndLineCap" Value="Flat" />
    <Setter Property="StrokeLineJoin" Value="Round" />
    <Setter Property="StrokeStartLineCap" Value="Flat" />
    <Setter Property="StrokeThickness" Value="2" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Width" Value="6" />
  </Style>
  <DropShadowEffect x:Key="controls.Ribbon.Values.RibbonTabControl.Content.DropShadowEffect" x:Shared="False" BlurRadius="3" ShadowDepth="0" Color="#FFCCCCCC" />
  <Storyboard x:Key="controls.Ribbon.Storyboards.RibbonTabControl.Content.Transition">
    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PART_CurrentContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
      <SplineDoubleKeyFrame KeyTime="00:00:00" Value="-10" />
      <SplineDoubleKeyFrame KeyTime="00:00:00.200" Value="0" />
    </DoubleAnimationUsingKeyFrames>
    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PART_CurrentContent" Storyboard.TargetProperty="Opacity">
      <SplineDoubleKeyFrame KeyTime="00:00:00" Value="0" />
      <SplineDoubleKeyFrame KeyTime="00:00:00.150" Value="1" />
    </DoubleAnimationUsingKeyFrames>
    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PART_PreviousContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
      <SplineDoubleKeyFrame KeyTime="00:00:00" Value="0" />
      <SplineDoubleKeyFrame KeyTime="00:00:00.050" Value="6" />
    </DoubleAnimationUsingKeyFrames>
    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PART_PreviousContent" Storyboard.TargetProperty="Opacity">
      <SplineDoubleKeyFrame KeyTime="00:00:00" Value="1" />
      <SplineDoubleKeyFrame KeyTime="00:00:00.050" Value="0" />
    </DoubleAnimationUsingKeyFrames>
  </Storyboard>
  <DataTemplate x:Key="controls.Ribbon.DataTemplates.RibbonGroupBox.OneLineHeader">
    <TextBlock HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding}" TextAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
  </DataTemplate>
  <DataTemplate x:Key="controls.Ribbon.DataTemplates.RibbonGroupBox.TwoLineHeader">
    <controls:TwoLineLabel HorizontalAlignment="Center" VerticalAlignment="Center" HasGlyph="True" Text="{Binding}" />
  </DataTemplate>
  <Style x:Key="controls.Ribbon.Styles.RibbonGroupBox.TwoLineHeaderContentControl" TargetType="{x:Type ContentControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ContentControl}">
          <ContentPresenter controls:RibbonGroupBox.IsCollapsedHeaderContentPresenter="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="controls.Ribbon.Styles.SeparatorTabItem" TargetType="controls:SeparatorTabItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="controls:SeparatorTabItem">
          <ContentPresenter ContentSource="Header" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="DefaultTransitioningControlStyle" TargetType="controls:TransitioningControl">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="controls:TransitioningControl">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Grid>
              <ContentPresenter x:Name="PART_CurrentContent" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                <ContentPresenter.RenderTransform>
                  <TransformGroup>
                    <ScaleTransform />
                    <SkewTransform />
                    <RotateTransform />
                    <TranslateTransform />
                  </TransformGroup>
                </ContentPresenter.RenderTransform>
              </ContentPresenter>
              <ContentPresenter x:Name="PART_PreviousContent" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                <ContentPresenter.RenderTransform>
                  <TransformGroup>
                    <ScaleTransform />
                    <SkewTransform />
                    <RotateTransform />
                    <TranslateTransform />
                  </TransformGroup>
                </ContentPresenter.RenderTransform>
              </ContentPresenter>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="DefaultFocusVisualRectangleStyle" TargetType="{x:Type Rectangle}">
    <Setter Property="Margin" Value="0" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Stroke" Value="{DynamicResource controls.Ribbon.Brushes.Black}" />
    <Setter Property="StrokeThickness" Value="1" />
  </Style>
  <ControlTemplate x:Key="controls.Ribbon.Templates.FocusVisual" TargetType="{x:Type Control}">
    <Rectangle Style="{DynamicResource DefaultFocusVisualRectangleStyle}" />
  </ControlTemplate>
  <Style x:Key="DefaultFocusVisualEmptyStyle" TargetType="{x:Type Control}">
    <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.FocusVisual.Empty}" />
  </Style>
  <Style x:Key="DefaultFocusVisualStyle" TargetType="{x:Type Control}">
    <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.FocusVisual}" />
  </Style>
  <ControlTemplate x:Key="controls.Ribbon.Templates.MenuSeparator" TargetType="{x:Type Separator}">
    <Grid Height="Auto">
      <Path Height="1" Margin="32 1 3 2" VerticalAlignment="Center" Data="M0,0L1,0" Fill="{x:Null}" Stretch="Fill" Stroke="{DynamicResource controls.Ribbon.Brushes.Separator.Border}" StrokeDashArray="2 2" />
    </Grid>
  </ControlTemplate>
  <Style x:Key="DefaultRibbonStyle" TargetType="{x:Type controls:Ribbon}">
    <Setter Property="internal:FrameworkHelper.UseLayoutRounding" Value="True" />
    <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Template" Value="{DynamicResource controls.Templates.Ribbon}" />
  </Style>
  <Style BasedOn="{StaticResource DefaultRibbonStyle}" TargetType="{x:Type controls:Ribbon}" />
  <Style BasedOn="{StaticResource DefaultTransitioningControlStyle}" TargetType="{x:Type controls:TransitioningControl}" />
  <ControlTemplate x:Key="controls.Templates.RibbonGroupBox" TargetType="{x:Type controls:RibbonGroupBox}">
    <Grid x:Name="rootGrid" HorizontalAlignment="Stretch">
      <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" />
      <ContentControl x:Name="nonCollapsedStateContainer" Focusable="False">
        <Grid x:Name="PART_ParentPanel">
          <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <controls:RibbonGroupBoxWrapPanel x:Name="PART_UpPanel" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Stretch" IsItemsHost="True" IsSimplified="{TemplateBinding IsSimplified}" />
          <Grid x:Name="PART_DownGrid" Grid.Row="1" MaxWidth="{Binding ActualWidth, ElementName=PART_ParentPanel}" Margin="0 0 2 2" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ContentControl x:Name="PART_HeaderContentControl" Margin="2 0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Foreground="{DynamicResource controls.Ribbon.Brushes.RibbonGroupBox.Header.Foreground}" Content="{TemplateBinding Header}" ContentStringFormat="{TemplateBinding HeaderStringFormat}" ContentTemplate="{TemplateBinding HeaderTemplate}" ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}" KeyboardNavigation.IsTabStop="False" />
            <controls:Button x:Name="PART_DialogLauncherButton" Grid.Column="1" Width="16" Height="16" Margin="0 0 2 0" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Background="Transparent" Icon="{TemplateBinding LauncherIcon}" controls:RibbonProperties.IconSize="Small" BorderBrush="Transparent" Command="{Binding LauncherCommand, RelativeSource={RelativeSource TemplatedParent}}" CommandParameter="{Binding LauncherCommandParameter, RelativeSource={RelativeSource TemplatedParent}}" CommandTarget="{Binding LauncherCommandTarget, RelativeSource={RelativeSource TemplatedParent}}" IsEnabled="{Binding IsLauncherEnabled, RelativeSource={RelativeSource TemplatedParent}}" Template="{DynamicResource controls.Ribbon.Templates.DialogLauncherButton}" ToolTip="{Binding LauncherToolTip, RelativeSource={RelativeSource TemplatedParent}}" Visibility="Visible" />
          </Grid>
        </Grid>
      </ContentControl>
      <Grid x:Name="collapsedStateGrid" Grid.RowSpan="2" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" Visibility="Collapsed">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border x:Name="collapsedIconBorder" Grid.Row="0" Grid.RowSpan="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="5 3 5 0" Padding="4" HorizontalAlignment="Center" VerticalAlignment="Top" BorderThickness="0" />
        <ContentControl x:Name="PART_CollapsedHeaderContentControl" Style="{DynamicResource controls.Ribbon.Styles.RibbonGroupBox.TwoLineHeaderContentControl}" Grid.Row="1" Grid.RowSpan="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="5 0 5 0" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="{DynamicResource controls.Ribbon.Brushes.RibbonGroupBox.Header.Foreground}" controls:RibbonGroupBox.IsCollapsedHeaderContentPresenter="True" Content="{TemplateBinding Header}" ContentStringFormat="{TemplateBinding HeaderStringFormat}" ContentTemplate="{TemplateBinding HeaderTemplate}" ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}" KeyboardNavigation.IsTabStop="False" />
        <Popup x:Name="PART_Popup" Height="{Binding ActualHeight, RelativeSource={RelativeSource TemplatedParent}}" AllowsTransparency="True" IsOpen="{TemplateBinding IsDropDownOpen}" PlacementTarget="{Binding ElementName=collapsedStateGrid, Mode=OneWay}">
          <Border x:Name="border" Background="{DynamicResource controls.Ribbon.Brushes.RibbonTabControl.Content.Background}" BorderBrush="{DynamicResource controls.Ribbon.Brushes.RibbonTabItem.Border}" BorderThickness="1" d:LayoutOverrides="Width">
            <ContentControl x:Name="popupContent" Margin="{TemplateBinding Padding}" Focusable="False" />
          </Border>
        </Popup>
      </Grid>
      <Rectangle x:Name="separator" Grid.RowSpan="2" Width="1" Margin="0 4" HorizontalAlignment="Right" VerticalAlignment="Stretch" Fill="{DynamicResource NavigationViewItemSeparatorForeground}" Visibility="{Binding IsSeparatorVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}" />
      <Image x:Name="PART_SnappedImage" Stretch="Uniform" Visibility="Collapsed" />
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="IsSimplified" Value="True">
        <Setter TargetName="PART_CollapsedHeaderContentControl" Property="Grid.Column" Value="1" />
        <Setter TargetName="PART_CollapsedHeaderContentControl" Property="Grid.ColumnSpan" Value="1" />
        <Setter TargetName="PART_CollapsedHeaderContentControl" Property="Grid.RowSpan" Value="2" />
        <Setter TargetName="PART_DownGrid" Property="Visibility" Value="Collapsed" />
        <Setter TargetName="PART_ParentPanel" Property="Height" Value="Auto" />
        <Setter TargetName="PART_UpPanel" Property="Margin" Value="0" />
        <Setter TargetName="PART_UpPanel" Property="Orientation" Value="Horizontal" />
        <Setter TargetName="collapsedIconBorder" Property="Grid.ColumnSpan" Value="1" />
        <Setter TargetName="collapsedIconBorder" Property="Grid.RowSpan" Value="2" />
        <Setter TargetName="collapsedIconBorder" Property="Margin" Value="0" />
      </Trigger>
      <Trigger Property="State" Value="Collapsed">
        <Setter TargetName="collapsedStateGrid" Property="Visibility" Value="Visible" />
        <Setter TargetName="nonCollapsedStateContainer" Property="Content" Value="{x:Null}" />
        <Setter TargetName="nonCollapsedStateContainer" Property="UIElement.Visibility" Value="Collapsed" />
        <Setter TargetName="popupContent" Property="Content" Value="{Binding ElementName=PART_ParentPanel}" />
        <Setter TargetName="rootGrid" Property="HorizontalAlignment" Value="Left" />
      </Trigger>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="State" Value="Collapsed" />
          <Condition Property="IsMouseOver" Value="True" />
        </MultiTrigger.Conditions>
        <Setter Property="Background" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Background}" />
      </MultiTrigger>
      <Trigger Property="IsDropDownOpen" Value="True">
        <Setter Property="Background" Value="{DynamicResource controls.Ribbon.Brushes.Button.Pressed.Background}" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="DefaultRibbonGroupBoxStyle" TargetType="{x:Type controls:RibbonGroupBox}">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="controls:RibbonProperties.IconSize" Value="Small" />
    <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultFocusVisualStyle}" />
    <Setter Property="Foreground" Value="{DynamicResource controls.Ribbon.Brushes.LabelText}" />
    <Setter Property="HeaderTemplateSelector" Value="{Binding Source={x:Static templateSelectors:RibbonGroupBoxHeaderTemplateSelector.Instance}}" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Padding" Value="4 2 4 2" />
    <Setter Property="Template" Value="{DynamicResource controls.Templates.RibbonGroupBox}" />
    <Style.Triggers>
      <Trigger Property="IsSimplified" Value="True">
        <Setter Property="controls:RibbonProperties.IconSize" Value="Medium" />
        <Setter Property="Padding" Value="2 0" />
      </Trigger>
      <Trigger Property="State" Value="QuickAccess">
        <Setter Property="controls:RibbonProperties.IconSize" Value="Small" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Height" Value="22" />
        <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.RibbonGroupBoxDropDownButton}" />
        <Setter Property="Width" Value="22" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <ControlTemplate x:Key="controls.Ribbon.Templates.ScrollButton" TargetType="{x:Type RepeatButton}">
    <Border Background="{DynamicResource controls.Ribbon.Brushes.ScrollViewer.Button.Background}" CornerRadius="{Binding Path=(controls:RibbonProperties.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
      <Border x:Name="OuterBorder" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{DynamicResource controls.Ribbon.Brushes.ScrollViewer.Button.Background}" BorderBrush="{DynamicResource controls.Ribbon.Brushes.ScrollViewer.Button.Border}" BorderThickness="1" CornerRadius="{Binding Path=(controls:RibbonProperties.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
      </Border>
    </Border>
    <ControlTemplate.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Background}" />
        <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Border}" />
      </Trigger>
      <Trigger Property="IsKeyboardFocusWithin" Value="True">
        <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Background}" />
        <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource controls.Ribbon.Brushes.Button.MouseOver.Border}" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="DefaultScrollButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="FocusVisualStyle" Value="{DynamicResource controls.Ribbon.Styles.FocusVisual.Empty}" />
    <Setter Property="Foreground" Value="{DynamicResource controls.Ribbon.Brushes.LabelText}" />
    <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.ScrollButton}" />
  </Style>
  <Style x:Key="DefaultRibbonScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
    <Setter Property="CanContentScroll" Value="True" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollViewer}">
          <Grid x:Name="Grid" Background="{TemplateBinding Background}" IsHitTestVisible="True">
            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Margin="{TemplateBinding Padding}" CanContentScroll="{TemplateBinding CanContentScroll}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" IsHitTestVisible="True" />
            <RepeatButton Style="{DynamicResource DynamicScrollBarLineButton}" Width="13" HorizontalAlignment="Left" VerticalAlignment="Stretch" Command="{x:Static ScrollBar.LineLeftCommand}" CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Focusable="false">
              <controls:RibbonProperties.CornerRadius>
                <Binding Converter="{x:Static conv:StaticConverters.CornerRadiusConverter}" Path="(controls:RibbonProperties.CornerRadius)" RelativeSource="{RelativeSource TemplatedParent}">
                  <Binding.ConverterParameter>
                    <conv:CornerRadiusPart>TopLeft,BottomLeft</conv:CornerRadiusPart>
                  </Binding.ConverterParameter>
                </Binding>
              </controls:RibbonProperties.CornerRadius>
              <RepeatButton.Visibility>
                <MultiBinding Converter="{StaticResource MenuScrollingVisibilityConverter}" ConverterParameter="0" FallbackValue="Visibility.Collapsed">
                  <Binding Path="ComputedHorizontalScrollBarVisibility" RelativeSource="{RelativeSource TemplatedParent}" />
                  <Binding Path="HorizontalOffset" RelativeSource="{RelativeSource TemplatedParent}" />
                  <Binding Path="ExtentWidth" RelativeSource="{RelativeSource TemplatedParent}" />
                  <Binding Path="ViewportWidth" RelativeSource="{RelativeSource TemplatedParent}" />
                </MultiBinding>
              </RepeatButton.Visibility>
              <Path Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.LeftRight}" Data="{DynamicResource controls.Ribbon.Values.Arrow.Left}" Stroke="{DynamicResource controls.Ribbon.Brushes.Black}" />
            </RepeatButton>
            <RepeatButton Style="{DynamicResource DefaultScrollButtonStyle}" Width="13" HorizontalAlignment="Right" VerticalAlignment="Stretch" Command="{x:Static ScrollBar.LineRightCommand}" CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Focusable="false">
              <controls:RibbonProperties.CornerRadius>
                <Binding Converter="{x:Static conv:StaticConverters.CornerRadiusConverter}" Path="(controls:RibbonProperties.CornerRadius)" RelativeSource="{RelativeSource TemplatedParent}">
                  <Binding.ConverterParameter>
                    <conv:CornerRadiusPart>TopRight,BottomRight</conv:CornerRadiusPart>
                  </Binding.ConverterParameter>
                </Binding>
              </controls:RibbonProperties.CornerRadius>
              <RepeatButton.Visibility>
                <MultiBinding Converter="{StaticResource MenuScrollingVisibilityConverter}" ConverterParameter="100" FallbackValue="Visibility.Collapsed">
                  <Binding Path="ComputedHorizontalScrollBarVisibility" RelativeSource="{RelativeSource TemplatedParent}" />
                  <Binding Path="HorizontalOffset" RelativeSource="{RelativeSource TemplatedParent}" />
                  <Binding Path="ExtentWidth" RelativeSource="{RelativeSource TemplatedParent}" />
                  <Binding Path="ViewportWidth" RelativeSource="{RelativeSource TemplatedParent}" />
                </MultiBinding>
              </RepeatButton.Visibility>
              <Path Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.LeftRight}" Data="{DynamicResource controls.Ribbon.Values.Arrow.Right}" Stroke="{DynamicResource controls.Ribbon.Brushes.Black}" />
            </RepeatButton>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
  </Style>
  <Style x:Key="DefaultRibbonGroupsContainerScrollViewerStyles" TargetType="{x:Type controls:RibbonGroupsContainerScrollViewer}" BasedOn="{StaticResource DefaultRibbonScrollViewerStyle}">
    <Setter Property="controls:RibbonProperties.CornerRadius" Value="{DynamicResource RibbonCornerRadius}" />
  </Style>
  <ControlTemplate x:Key="controls.Templates.RibbonTabControl" TargetType="{x:Type controls:RibbonTabControl}">
    <Grid x:Name="ContentGrid" VerticalAlignment="Top" Background="{TemplateBinding Background}" KeyboardNavigation.TabNavigation="Local">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition x:Name="contentRow" MinHeight="{TemplateBinding ContentHeight}" MaxHeight="{TemplateBinding ContentHeight}" />
        <RowDefinition x:Name="contentGapRow" MinHeight="{TemplateBinding ContentGapHeight}" MaxHeight="{TemplateBinding ContentGapHeight}" />
      </Grid.RowDefinitions>
      <!-- Tab -->
      <Grid x:Name="TabsGrid" Grid.Row="0" Margin="0,0,0,4" HorizontalAlignment="Stretch" Background="{TemplateBinding Background}" KeyboardNavigation.TabIndex="1" TextElement.Foreground="{TemplateBinding Foreground}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ContentPresenter Grid.Column="0" VerticalAlignment="Stretch" ContentSource="Menu" />
        <controls:RibbonScrollViewer Style="{DynamicResource DefaultRibbonTabScrollViewerStyle}" Grid.Column="1" Margin="0" HorizontalAlignment="Left" HorizontalScrollBarVisibility="Auto" IsHitTestVisible="True" IsTabStop="False" VerticalScrollBarVisibility="Disabled" Visibility="{Binding AreTabHeadersVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}">
          <controls:RibbonTabsContainer x:Name="PART_TabsContainer" HorizontalAlignment="Stretch" IsItemsHost="True" KeyboardNavigation.TabIndex="1" />
        </controls:RibbonScrollViewer>
      </Grid>
      <!--Content-->
      <Border Grid.Row="1" Margin="8,0,8,0" HorizontalAlignment="Stretch" Background="{DynamicResource NavigationViewContentBackground}" BorderBrush="{StaticResource NavigationViewContentGridBorderBrush}" BorderThickness="0" CornerRadius="{DynamicResource RibbonCornerRadius}" KeyboardNavigation.DirectionalNavigation="Contained" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local" TextElement.Foreground="{TemplateBinding Foreground}">
        <Grid>
          <!-- We must use a dedicated border to prevent the effect from affecting font rendering... -->
          <Border Background="{DynamicResource NavigationViewItemBackground}" BorderBrush="{DynamicResource NavigationViewContentGridBorderBrush}" BorderThickness="0" CornerRadius="{DynamicResource RibbonCornerRadius}">
            <Border.Effect>
              <DropShadowEffect BlurRadius="20" Direction="270" Opacity="0.25" ShadowDepth="6" />
            </Border.Effect>
          </Border>
          <ContentControl x:Name="mainContentControl" Margin="0" Focusable="False">
            <controls:TransitioningControl x:Name="PART_SelectedContent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" NextContent="{Binding SelectedContent, RelativeSource={RelativeSource TemplatedParent}}" TransitionStoryboard="{DynamicResource ui.Ribbon.Storyboards.RibbonTabControl.Content.Transition}" />
          </ContentControl>
        </Grid>
      </Border>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="IsMinimized" Value="True">
        <Setter TargetName="contentGapRow" Property="MinHeight" Value="0" />
        <Setter TargetName="contentRow" Property="MinHeight" Value="0" />
        <!-- Switch content to popup -->
        <Setter TargetName="mainContentControl" Property="Content" Value="{x:Null}" />
        <!-- Switch content to popup END -->
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="DefaultRibbonTabControlStyle" TargetType="{x:Type controls:RibbonTabControl}">
    <Setter Property="Background" Value="{DynamicResource Background}" />
    <Setter Property="CanMinimize" Value="{Binding CanMinimize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Ribbon}}, Mode=TwoWay}" />
    <Setter Property="CanUseSimplified" Value="{Binding CanUseSimplified, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Ribbon}}, Mode=TwoWay}" />
    <Setter Property="ContentGapHeight" Value="{Binding ContentGapHeight, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Ribbon}}}" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Foreground" Value="{DynamicResource CardForeground}" />
    <Setter Property="IsMinimized" Value="{Binding IsMinimized, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Ribbon}}, Mode=TwoWay}" />
    <Setter Property="IsSimplified" Value="{Binding IsSimplified, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type controls:Ribbon}}, Mode=TwoWay}" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Template" Value="{DynamicResource controls.Templates.RibbonTabControl}" />
  </Style>
  <ControlTemplate x:Key="controls.Templates.RibbonTabItem" TargetType="{x:Type controls:RibbonTabItem}">
    <Grid x:Name="grid">
      <Border x:Name="groupBorder" Margin="0 0 0 1" Background="{Binding Group.Background, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=Transparent}" BorderThickness="0" OpacityMask="#14000000" />
      <Border x:Name="backgroundBorder" Background="{TemplateBinding Background}" BorderThickness="0" />
      <!--<Border x:Name="underlineContainerBorder"
                   Margin="0 0 0 3"
                   Background="{TemplateBinding ActiveTabBackground}"
                   Visibility="Collapsed">
               <Border x:Name="underlineBorder"
                       Margin="{Binding HeaderPadding, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static conv:ExtractLeftRightFromThicknessConverter.Default}}"
                       BorderBrush="{TemplateBinding ActiveTabBorderBrush}"
                       BorderThickness="0 0 0 2"
                       Visibility="Collapsed" />
           </Border>-->
      <Border x:Name="underlineBorder" Margin="{Binding HeaderPadding, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static conv:ExtractLeftRightFromThicknessConverter.Default}}" BorderBrush="{TemplateBinding ActiveTabBorderBrush}" BorderThickness="0 0 0 3" Visibility="Collapsed" />
      <Border x:Name="PART_ContentContainer" Padding="{TemplateBinding HeaderPadding}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ContentControl x:Name="PART_HeaderContentHost" Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}" ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}" Focusable="False" />
      </Border>
      <Grid x:Name="separatorGrid" Width="1" Margin="0 4 0 4" HorizontalAlignment="Right" Opacity="{TemplateBinding SeparatorOpacity}">
        <Rectangle Width="1" Fill="#FFD8D8D9" Stroke="#FFD8D8D9" />
      </Grid>
    </Grid>
    <ControlTemplate.Triggers>
      <Trigger Property="IsContextual" Value="False">
        <Setter Property="BorderBrush" Value="blue" />
      </Trigger>
      <Trigger Property="IsContextual" Value="True">
        <Setter Property="Foreground" Value="{Binding Group.TabItemForeground, RelativeSource={RelativeSource Self}}" />
      </Trigger>
      <Trigger Property="IsSelected" Value="True">
        <Setter Property="controls:RibbonProperties.MouseOverForeground" Value="{DynamicResource TabViewItemForegroundSelected}" />
        <Setter TargetName="underlineBorder" Property="BorderBrush" Value="{DynamicResource NavigationViewSelectionIndicatorForeground}" />
        <Setter TargetName="underlineBorder" Property="Visibility" Value="Visible" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter TargetName="PART_ContentContainer" Property="Opacity" Value="0.5" />
      </Trigger>
      <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=controls:RibbonTabControl}, Path=SelectedItem}" Value="{x:Null}">
        <Setter TargetName="groupBorder" Property="Margin" Value="0" />
      </DataTrigger>
      <Trigger Property="Group" Value="{x:Null}">
        <Setter TargetName="groupBorder" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="underlineBorder" Property="BorderBrush" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
        <Setter TargetName="underlineBorder" Property="Visibility" Value="Visible" />
        <!--<Setter TargetName="underlineContainerBorder" Property="Visibility" Value="Visible" />-->
      </Trigger>
      <Trigger Property="Selector.IsSelected" Value="True">
        <Setter TargetName="underlineBorder" Property="BorderBrush" Value="{DynamicResource NavigationViewSelectionIndicatorForeground}" />
        <Setter TargetName="underlineBorder" Property="Visibility" Value="Visible" />
        <!--<Setter TargetName="underlineContainerBorder" Property="Visibility" Value="Visible" />-->
      </Trigger>
      <!--Foreground for Selected, with group-->
      <Trigger Property="Selector.IsSelected" Value="True">
        <Setter Property="Foreground" Value="{Binding Group.TabItemSelectedForeground, RelativeSource={RelativeSource Self}}" />
      </Trigger>
      <!--Foreground for selected, no group-->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Selector.IsSelected" Value="True" />
          <Condition Property="IsContextual" Value="False" />
        </MultiTrigger.Conditions>
        <Setter Property="Foreground" Value="{DynamicResource TabViewItemForegroundSelected}" />
      </MultiTrigger>
      <!--Background and foreground for MouseOver, with group-->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition SourceName="grid" Property="IsMouseOver" Value="True" />
        </MultiTrigger.Conditions>
        <!--<Setter Property="Background" Value="{Binding Path=(ui:RibbonProperties.MouseOverBackground), RelativeSource={RelativeSource Self}}" />-->
        <Setter Property="Foreground" Value="{Binding Group.TabItemMouseOverForeground, RelativeSource={RelativeSource Self}}" />
      </MultiTrigger>
      <!--Foreground for selected MouseOver, with group-->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Selector.IsSelected" Value="True" />
          <Condition Property="IsMouseOver" Value="True" />
          <Condition SourceName="grid" Property="IsMouseOver" Value="True" />
        </MultiTrigger.Conditions>
        <Setter Property="Foreground" Value="{Binding Group.TabItemSelectedMouseOverForeground, RelativeSource={RelativeSource Self}}" />
        <MultiTrigger.EnterActions>
          <BeginStoryboard x:Name="beginStoryboardSelectedMouseOver">
            <Storyboard>
              <ThicknessAnimation Storyboard.TargetName="underlineBorder" Storyboard.TargetProperty="Margin" To="0" Duration="0:0:0.1" />
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.EnterActions>
        <MultiTrigger.ExitActions>
          <BeginStoryboard>
            <Storyboard>
              <ThicknessAnimation Storyboard.TargetName="underlineBorder" Storyboard.TargetProperty="Margin" Duration="0:0:0.1" />
            </Storyboard>
          </BeginStoryboard>
        </MultiTrigger.ExitActions>
      </MultiTrigger>
      <!--Background and foreground for MouseOver, no group-->
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition SourceName="grid" Property="IsMouseOver" Value="True" />
          <Condition Property="Group" Value="{x:Null}" />
        </MultiTrigger.Conditions>
        <!--<Setter Property="Background" Value="{Binding Path=(ui:RibbonProperties.MouseOverBackground), RelativeSource={RelativeSource Self}}" />-->
        <Setter Property="Foreground" Value="{Binding Path=(controls:RibbonProperties.MouseOverForeground), RelativeSource={RelativeSource Self}}" />
      </MultiTrigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="DefaultRibbonTabItemStyle" TargetType="{x:Type controls:RibbonTabItem}">
    <Setter Property="ActiveTabBackground" Value="{DynamicResource NavigationViewItemBackgroundSelected}" />
    <Setter Property="ActiveTabBorderBrush" Value="{DynamicResource TabViewSelectedItemBorderBrush}" />
    <Setter Property="BorderBrush" Value="{DynamicResource NavigationViewItemBorderBrush}" />
    <Setter Property="controls:RibbonProperties.MouseOverBackground" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
    <Setter Property="controls:RibbonProperties.MouseOverForeground" Value="{DynamicResource NavigationViewItemForegroundPointerOver}" />
    <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
    <Setter Property="Foreground" Value="{DynamicResource NavigationViewItemForeground}" />
    <Setter Property="HeaderTemplate">
      <Setter.Value>
        <DataTemplate>
          <Grid>
            <!-- Hidden TextBlock is used to prevent width differences due to FontWeight changes when item is selected -->
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Text="{Binding}" TextWrapping="NoWrap" Visibility="Hidden" />
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding}" TextWrapping="NoWrap">
              <TextBlock.Style>
                <Style>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=controls:RibbonTabItem}}" Value="True">
                      <Setter Property="TextElement.FontWeight" Value="Bold" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </Grid>
        </DataTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="Margin" Value="0" />
    <Setter Property="Template" Value="{DynamicResource controls.Templates.RibbonTabItem}" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Style.Triggers>
      <MultiDataTrigger>
        <MultiDataTrigger.Conditions>
          <Condition Binding="{Binding IsContextual, RelativeSource={RelativeSource Self}}" Value="True" />
          <Condition Binding="{Binding Group.Visibility, RelativeSource={RelativeSource Self}}" Value="Collapsed" />
        </MultiDataTrigger.Conditions>
        <Setter Property="Visibility" Value="Collapsed" />
      </MultiDataTrigger>
    </Style.Triggers>
  </Style>
  <ControlTemplate x:Key="controls.Ribbon.Templates.TwoLineLabel" TargetType="{x:Type controls:TwoLineLabel}">
    <StackPanel x:Name="stackPanel" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
      <AccessText x:Name="PART_TextRun" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
      <StackPanel x:Name="stackPanel1" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
        <AccessText x:Name="PART_TextRun2" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" />
        <Border x:Name="border" Margin="0" HorizontalAlignment="Center">
          <Path x:Name="path" Style="{DynamicResource controls.Ribbon.Styles.Path.Arrow.UpDown}" Margin="0 2 0 0" Data="{DynamicResource controls.Ribbon.Values.Arrow.Down}" Stroke="{TemplateBinding Foreground}" />
        </Border>
      </StackPanel>
    </StackPanel>
    <ControlTemplate.Triggers>
      <Trigger Property="HasTwoLines" Value="False">
        <Setter TargetName="PART_TextRun2" Property="Visibility" Value="Collapsed" />
        <Setter TargetName="border" Property="Margin" Value="0" />
        <Setter TargetName="border" Property="VerticalAlignment" Value="Center" />
        <Setter TargetName="stackPanel" Property="HorizontalAlignment" Value="Left" />
        <Setter TargetName="stackPanel" Property="Orientation" Value="Horizontal" />
        <Setter TargetName="stackPanel" Property="VerticalAlignment" Value="Center" />
        <Setter TargetName="stackPanel1" Property="Margin" Value="0 0 0 0" />
        <Setter TargetName="stackPanel1" Property="VerticalAlignment" Value="Center" />
      </Trigger>
      <Trigger Property="HasGlyph" Value="False">
        <Setter TargetName="border" Property="Width" Value="0.1" />
        <Setter TargetName="path" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger SourceName="PART_TextRun" Property="Text" Value="">
        <Setter TargetName="PART_TextRun" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger SourceName="PART_TextRun" Property="Text" Value="{x:Null}">
        <Setter TargetName="PART_TextRun" Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger SourceName="PART_TextRun2" Property="Text" Value="">
        <Setter TargetName="PART_TextRun2" Property="Visibility" Value="Hidden" />
      </Trigger>
      <Trigger SourceName="PART_TextRun2" Property="Text" Value="{x:Null}">
        <Setter TargetName="PART_TextRun2" Property="Visibility" Value="Hidden" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  <Style x:Key="controls.Ribbon.Styles.MenuSeparator" TargetType="{x:Type Separator}">
    <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.MenuSeparator}" />
  </Style>
  <Style BasedOn="{StaticResource DefaultRibbonTabControlStyle}" TargetType="{x:Type controls:RibbonTabControl}" />
  <Style BasedOn="{StaticResource DefaultRibbonTabItemStyle}" TargetType="{x:Type controls:RibbonTabItem}" />
  <Style BasedOn="{StaticResource DefaultRibbonGroupBoxStyle}" TargetType="{x:Type controls:RibbonGroupBox}" />
  <Style BasedOn="{StaticResource DefaultRibbonGroupsContainerScrollViewerStyles}" TargetType="{x:Type controls:RibbonGroupsContainerScrollViewer}" />
  <Style x:Key="DefaultRibbonTwoLineLabelStyle" TargetType="{x:Type controls:TwoLineLabel}">
    <Setter Property="Template" Value="{DynamicResource controls.Ribbon.Templates.TwoLineLabel}" />
  </Style>
  <Style BasedOn="{StaticResource DefaultRibbonTwoLineLabelStyle}" TargetType="{x:Type controls:TwoLineLabel}" />
</ResourceDictionary>