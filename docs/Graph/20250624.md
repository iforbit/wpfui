# Project Dev Log (WPF.UI.DirectX)

## 📅 2025-06-24: 폴더 구조 정비 및 로컬 데이터 자동스크롤 /  외부데이터 입력구조 테스트 

### ✅ 구조 리팩토링 및 주요 변경점

* 프로젝트 폴더 정리
* Vertex Shader 수정 - Transform 기반으로 스케일, offset 정보 입력
* `GetSpanMergedUnsafeCached()`  - Span 정보관련 getter
* `FollowLatestX()` 자동 스크롤 기능 테스트 및 튜닝
* `SignalGenerator`외부에서 조정가능한 sin, cos 파장 생성 프로그램 개발 - SHM을 통한 통신
* `AutoAdjustTransform()` x축 자동스크롤 과 Y축 자동 스케일링 기능을 포함한 함수

### 06/24 폴더구조
```plaintext
Wpf.Ui.DirectX/
├─ Core/
│  └─ IGraphSeries.cs, IRenderable.cs, IRenderStyleKeyProvider.cs
├─ Controls/
│  └─ GraphControl.cs, LegendPanel.xaml
├─ Helpers/
│  └─ VertexBufferFactory.cs
├─ Models/
│  └─ GraphSeries.cs,RealTimeSeries.cs, AxisManager.cs, VertexTypes/, Buffers/
├─ Rendering/
│  └─ D3D11Renderer.cs, SeriesRendererBase.cs, RealTimeSeriesRenderer.cs, SeriesRendererManager.cs, ShaderManager.cs, DrawGroupBatcher.cs Interop/ 
│  └─ Transform/
│     └─ TransformManager.cs, ViewProjectionBuffer.cs
├─ Services/
│  └─ GraphicsService.cs, RenderThreadService.cs
├─ Extensions/
│  └─ ChartModifier, AnnotationLayer
├─ Shaders/
│  └─ LinePixelShader.hlsl, LineVertexShader.hlsl, ConstPixelShader.hlsl,ConstVertexShader.hlsl
└─ README.md
```

### 🎨 SignalGenerator

* WPF MVVM Sample 사용
* `MemoryMappedFile` 및 `Task`를 사용한  `SignalEngine.cs` → `SignalChannel.cs` 작성
* UI 를 통하여 Frequency, Amplitude, Phase, Sine/Cos Type 설정

### 🔍 테스트 및 검증

* `SignalGenerator`에서 입력한 SHM의 데이터르 가지고 그래프 그리기
* 1보다 작은 Y값을 화면에 표현못하여 자동 Y Scaling 기능 구현 시작
* x축 스크롤과 y축 scaling을 통합한 `AutoAdjustTransform()` 제작
* 기능 미구현 및 튜닝 단계 

### 🧹 기타

* 자동 계산 이전에 스크롤바 이동과 휠 줌인/아웃을 구현하여 원하는 화면 좌표 이동을 우선 구현하고 그기능을 이용하여 화면 자동 스크롤 및 스케일링을 진행하는게 타당한듯 

---

📌 내일은 자동 스크롤 기준점 보정 및 XOffset의 초기 기준점을 보존하면서도 시각적으로 자연스럽게 이동하는 구조를 마무리하는 것이 우선입니다.
